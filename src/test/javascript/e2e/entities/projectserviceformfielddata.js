'use strict';

describe('Projectserviceformfielddata e2e test', function () {

    var username = element(by.id('username'));
    var password = element(by.id('password'));
    var entityMenu = element(by.id('entity-menu'));
    var accountMenu = element(by.id('account-menu'));
    var login = element(by.id('login'));
    var logout = element(by.id('logout'));

    beforeAll(function () {
        browser.get('/');

        accountMenu.click();
        login.click();

        username.sendKeys('admin');
        password.sendKeys('admin');
        element(by.css('button[type=submit]')).click();
    });

    it('should load Projectserviceformfielddata', function () {
        entityMenu.click();
        element.all(by.css('[ui-sref="projectserviceformfielddata"]')).first().click().then(function() {
            element.all(by.css('h2')).first().getAttribute('data-translate').then(function (value) {
                expect(value).toMatch(/investhryApp.projectserviceformfielddata.home.title/);
            });
        });
    });

    it('should load create Projectserviceformfielddata dialog', function () {
        element(by.css('[ui-sref="projectserviceformfielddata.new"]')).click().then(function() {
            element(by.css('h4.modal-title')).getAttribute('data-translate').then(function (value) {
                expect(value).toMatch(/investhryApp.projectserviceformfielddata.home.createOrEditLabel/);
            });
            element(by.css('button.close')).click();
        });
    });

    afterAll(function () {
        accountMenu.click();
        logout.click();
    });
});
